{
  "name": "dnd-kit-tree-workspace",
  "author": "",
  "license": "MIT",
  "packageManager": "pnpm@10.6.3",
  "scripts": {
    "build": "pnpm run --filter=dnd-kit-tree build",
    "dev": "pnpm run --filter=dnd-kit-tree build --watch",
    "storybook:dev": "pnpm run --filter=storybook storybook",
    "npm-publish": "pnpm run build && pnpm run --filter=dnd-kit-tree npm-publish",
    "storybook:build": "pnpm run build && pnpm run --filter=storybook build-storybook"
  }
}